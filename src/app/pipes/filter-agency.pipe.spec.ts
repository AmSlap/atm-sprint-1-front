import { FilterAgencyPipe } from './filter-agency.pipe';

describe('FilterAgencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAgencyPipe();
    expect(pipe).toBeTruthy();
  });
});
